# `direnv` and Nix are the only bootstrap requirements. Every project tool is
# supplied by flake.nix. Using `nix print-dev-env` keeps this independent of a
# globally installed nix-direnv integration.
watch_file flake.nix
watch_file flake.lock
eval "$(nix print-dev-env)"
