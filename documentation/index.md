# red-table documentation

This site contains the English and German user manuals plus configuration and
thumbnail-cache references for `red-table`.

`red-table` is a fast, keyboard-driven terminal image browser for inspecting,
comparing, and selecting images in large collections.
